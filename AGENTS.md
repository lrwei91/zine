# AGENTS.md

## 项目范围

本仓库维护“拾页 zine”静态展示站。站点参考 Zeejay Zine 的信息结构，以原创内容重新实现，并遵循 `project-standards` 当前的纸面油墨编辑风规范。

## 权威来源

- 产品范围、参考登记和设计判断以 `design-brief.md` 为准。
- 页面结构以 `index.html` 为准，视觉实现以 `styles.css` 为准，渐进增强交互以 `app.js` 为准。
- 跨项目美术规则以相邻仓库 `../project-standards/design/visual-style.md` 为准；项目约束与其冲突时，先记录到 `design-brief.md`，不要静默偏离。
- `README.md` 负责当前运行、验证、目录和部署说明，行为变化时同步更新。

## 实现规则

- 保持原生 HTML、CSS、JavaScript 的静态架构；没有明确收益时不引入框架、构建器或运行时依赖。
- 保持 `index.html` 为部署入口，静态媒体放在 `public/assets/`，不要把 `.vercel/` 本地链接信息提交到仓库。
- 账户、政策、生成记录和生成服务目前都是明确标注的本地演示；不得伪装成已接入的后端、上传、登录或持久化能力。
- 本地图片仅用于浏览器内校验与预览，不上传、不保存；改变这一边界前必须补充产品说明、安全设计和失败处理。
- 不复制参考站的 Logo、作品、账户信息、社会证明或专有文案。参考站只用于已登记的信息结构和位置关系。
- 修改时只处理当前需求，不顺手重构无关代码，不覆盖未提交的用户改动。

## 视觉与交互契约

- 固定浅色模式：微暖纸面、近黑油墨、单一荧光黄强调、细网格、细边框和编辑式留白。
- 组件优先消费 `styles.css` 中的语义 token，不在局部散落新的主色或重复定义视觉常量。
- 保留桌面固定侧栏、移动端底部导航、作品瀑布流和“开始创作”主入口的层级关系。
- 原生 `dialog` 必须支持 Escape、关闭后焦点返回和清晰的键盘焦点；不能只靠 hover 表达关键状态。
- JavaScript 关闭时，品牌说明和全部作品仍应可读；`prefers-reduced-motion: reduce` 下直接呈现稳定内容。
- 响应式至少检查 320、390、768、1024、1440 CSS px，不得出现横向溢出、标题裁切、导航遮挡或作品缺失。

## 验证

修改后至少运行：

```bash
npm run check
node --check app.js
git diff --check
```

涉及布局、交互或媒体时，还必须在真实浏览器中检查相关视口、对话框、键盘路径、减弱动效和控制台错误。涉及部署时，验证生产域名、HTTPS、首页和静态资源的实际响应；本地检查不能替代线上证据。

## 发布安全

- 生产环境使用 Vercel，公开域名为 `https://zine.lrwei91.cn`，配置入口为 `vercel.json`。
- 默认不提交、不推送、不重新部署，也不修改 DNS；只有用户明确要求时才执行对应外部写入。
- 推送前检查暂存范围、敏感信息、测试结果和远端分支状态；不要提交 `.vercel/`、凭据、缓存或临时截图。
